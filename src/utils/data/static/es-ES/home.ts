/**
 * @deprecated Home copy now lives in `i118builder/messages/home/**`.
 * Locale-independent home assets now live in `src/utils/data/static/home.ts`.
 */
export { homeAssets as homeData } from '../home'
