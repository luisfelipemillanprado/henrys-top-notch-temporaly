/**
 * @description Represents the properties for the WhatsApp contact section.
 * @export
 * @interface WhatsAppProps
 * @property {string} href - The URL that the WhatsApp contact section points to.
 */
export interface WhatsAppProps {
  href: string
}

/**
 * @description Represents the properties for the Phone contact section.
 * @export
 * @interface PhoneProps
 * @property {string} href - The URL that the Phone contact section points to.
 */
export interface PhoneProps {
  href: string
}
